package BaekJoon.q5622.Fail;

public class Fail3 {
    public static void main(String[] args) {
        /*for (int i = 0; i < tmp.length(); i++) {
            int inputIdx = tmp.charAt(i)-'A';
            if (!chk[inputIdx])
                chk[inputIdx] = true;
        }

        int result = 0;
        String pos = " ";
        for (int i = 0; i < chk.length; i++) {
            if (i >= 15 && i <= 18 && chk[i])
                pos += 7+"";
            else if (i >= 22 && i <= 25 && chk[i])
                pos += 9+"";
            else if (chk[i])
                pos += i/3+2+"";

            System.out.println(pos);
        }*/
    }
}
